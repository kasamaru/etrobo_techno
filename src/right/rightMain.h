#pragma once
#include <stdio.h>
#include "common/common.h"

class RightCource {
    public:
    void startAlwaysTask(void);
    void stopAlwaysTask(void);

    private:
    ColorSense ;
    Walker ;
}